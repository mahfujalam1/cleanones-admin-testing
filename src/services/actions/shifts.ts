import { authenticated } from "./auth";
export type ShiftRoomInput = { room_id: string; custom_room_name?: string; additional_tasks?: Array<{ name: string }>; additional_photo_requirements?: Array<{ name: string }>; duration?: number; clean_type?: string; tasks?: unknown[]; required_photos?: unknown[] };
export type ShiftDraftInput = { client_id: string; location_id: string; date: string; start_time: string; end_time: string; repeat_shift: string; shift_notes: string; cleaning_plan_id?: string; room_ids: string[]; rooms: ShiftRoomInput[] };
export type ShiftApi = { id: string; draft_id?: string; client_id: string; client_name: string; location_id: string; location_name: string; date: string; start_time: string; end_time: string; repeat_shift?: string; shift_notes: string; cleaning_plan_id: string; rooms: Array<{ room_id: string; room_name: string; status: string }>; total_tasks_count: number; total_photo_required: number; status: string; workers?: Array<{ worker_id: string; name: string; profile_picture: string; worker_type: string; shift_role: string }> };
const json = (value: unknown) => ({ headers: { "Content-Type": "application/json" }, body: JSON.stringify(value) });
